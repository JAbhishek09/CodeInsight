/**
 * user.routes.js
 *
 * Reserved for future user-management endpoints (e.g. update profile, change password).
 * Currently all user-related auth routes live in auth.routes.js.
 * This file is NOT mounted in server.js — it is a placeholder only.
 */
import express from 'express';
const router = express.Router();
export default router;
